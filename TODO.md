- [x] Add Inter font import (Google Fonts link) in `Index.html`.

- [ ] Update `src/index.css` typography defaults (font smoothing + line-height) without changing colors.
- [x] Align Tailwind `fontFamily.sans` to Inter in `tailwind.config.js`.

- [x] Rebuild (`npm run build`) to verify no errors.


